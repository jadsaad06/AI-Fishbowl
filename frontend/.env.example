# Frontend (Electron) environment variables.

# MCP client websocket/url endpoint consumed by Electron main/preload.
# Default is the local MCP server running on port 8000.
GCP_MCP_URL=ws://127.0.0.1:8000/text_input

# Optional API key exposed to the preload bridge.
API_NINJAS_KEY=
